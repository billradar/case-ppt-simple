---
name: case-ppt-simple
description: 病例汇报 PPT 保守改造（只增不改）。当需要在完全不改动现有 PPT 模板排版、字体、字号、颜色、母版、版式、页序与动画的前提下改造病例汇报，且内容可自行生成/补全、可新增图片表格文本时使用；交付按 NAME-科室-病例汇报 命名。与 case-report-ppt（重排版版）互为姊妹项目。
compatibility: opencode
metadata:
  format: agent-skills
  locale: zh-CN
---

# 病例汇报 PPT 保守改造（case-ppt-simple）

与 `case-report-ppt` 的差别只有两点：**排版更保守、内容更自由**。

- **排版（保守）**：只增不改。绝不改动现有模板的排版与格式；只替换既有文本框里的文字，或新增独立元素（图片、表格、文本框）。`layout_change_level` 固定为 0。
- **内容（自由）**：可以自行生成、补全病史、查体、检验、影像、诊断、治疗、转归等教学病例内容；不受"不补造"限制。仍不得上传或保留真实患者的标识信息。

本项目的目标是：**听众感觉不到模板被动过，只看到内容更清楚、更完整。**

## 约定常量

- 交付命名：`NAME-科室-病例汇报.pptx`（`NAME` 为汇报人，科室为轮转科室；不加病种/日期）。
- 工作/恢复副本：`NAME-科室-病例汇报_working.pptx`；交付后删除，只在目标目录留最终版与原件。
- 异常值强调色（可选）：`#EE0000`，且只对既有数值字符设色，不动字号/字体/位置。默认不着色。

## 开始

1. 先用文件读取能力加载 [references/workflow.md](references/workflow.md)；编辑前先创建并验证安全副本。
2. 盘点源 PPT：页数、页序、版式、母版、字体规格、可编辑文本框、图片、表格、组合对象、动画与切换；建立"不可动对象"清单。
3. 按任务读取对应参考文件：

| 情形 | 必读文件 |
| --- | --- |
| 工作流、增页、溢出处理、交付 | [references/workflow.md](references/workflow.md) |
| 允许新增什么、新增元素怎么放 | [references/layout-design.md](references/layout-design.md) |
| 内容生成、数值/日期一致、脱敏 | [references/medical-content.md](references/medical-content.md) |
| 工具操作、如何只改文字不改格式 | [references/powerpoint-technical.md](references/powerpoint-technical.md) |
| 完成前逐项核对 | [references/validation.md](references/validation.md) |
| 允许/禁止的具体例子 | [references/examples.md](references/examples.md) |

## 铁律（不可突破）

- 不移动、不缩放、不旋转、不删除、不组合或不组合任何**原有**对象。
- 不改字体、字号、字重、颜色、对齐、行距、缩进、项目符号、形状填充/边框/阴影/效果。
- 不改页面尺寸/比例、主题、主题色、背景、Logo、母版、版式、页眉页脚、页码。
- 不改页序；除授权外不增删页。增页必须复用与源页**完全相同**的版式与母版元素。
- 不增删动画与切换。
- 只允许两类改动：
  1. **就地改字**：替换既有文本框中的文字，保留其原有格式。
  2. **新增元素**：新增独立图片、表格、文本框/形状，置于空白区，不覆盖、不移动任何原对象。
- 唯一例外是**脱敏**：源中含真实可识别信息时，遮蔽或删除标识信息，并在交付说明中记录。

## 默认决策

- `layout_change_level = 0`（完全保持原 Layout）。
- 内容可生成：据科室与病种自行编写去标识化教学病例，无需"待核实"负担；数值、单位、日期在全篇保持一致即可。
- 文字溢出：优先**精简文字**；仍放不下就**新增一页**（复制既有页以保留版式），绝不缩小字号、绝不移动/缩放原对象。
- 表格/图片一律"新增"，不改动既有表格或图片；新增对象采用与全篇一致的字体族与字号区间（正文不低于 16 pt）。
- 幻灯片（正文、页脚、备注、图片替代文本）不出现"教学病例模拟数据""模拟数据""仅用于演示"等字样。

## 交付

- 保存为 `NAME-科室-病例汇报.pptx`；交付后删除 `_working` 副本，只保留最终版与原件。
- 汇报时说明：输出路径、新增了哪些元素/页面、是否有脱敏、以及"原模板排版与格式零改动"的核对结论。
