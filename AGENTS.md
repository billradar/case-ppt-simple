# AGENTS.md

给自动化代理的简明约束。完整规则见 [SKILL.md](SKILL.md) 与 `references/`。

## 这个仓库做什么

病例汇报 PPT 的**保守改造**：在**完全不改动**现有模板排版与格式的前提下，替换文字、新增图片/表格/文本框。内容可自行生成。

## 必须遵守

1. **先复制**源 PPT 为工作副本，只编辑副本，原件不动。
2. **只增不改**：
   - 允许：在既有文本框内改文字（保留原格式）；在空白区新增独立对象。
   - 禁止：移动/缩放/旋转/删除/组合任何原对象；改字体、字号、颜色、对齐、缩进、填充、边框；改母版、版式、主题、背景、页眉页脚、页码、页序；增删动画。
3. **溢出**：先精简文字，仍不够就**新增一页**（复制既有页保留版式）；绝不缩小字号。
4. **内容**：可生成/补全教学病例；如用真实资料必须先脱敏。
5. **措辞**：幻灯片上不出现"教学病例模拟数据/仅用于演示"等字样。
6. **命名**：`NAME-科室-病例汇报.pptx`；交付后删除 `_working` 副本。
7. **核对**：每批改动后回读原对象的 `left/top/width/height` 与字体/字号/颜色，确认零变化。

## 不要做

- 不要用"整体重写形状文本"的接口（会重置格式）。
- 不要用默认字体/主题/填充/线框等全局样式接口。
- 不要因为内容放不下就动模板。
