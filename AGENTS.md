# AGENTS.md

给自动化代理的简明约束。完整规则见 [SKILL.md](SKILL.md) 与 `references/`。

## 这个仓库做什么

病例汇报 PPT 的**保守改造**：**模板骨架零改动**，但文字内容、文本格式、既有表格与图片都可自由编辑，并可新增元素。内容可自行生成。

## 必须遵守

1. **先复制**源 PPT 为工作副本，只编辑副本，原件不动。
2. **不动（骨架）**：页面尺寸/比例、主题、主题色、背景、Logo、母版、版式、页眉页脚、页码、页序、动画与切换。
3. **不动（几何）**：表格与图片**以外**的原有对象——不移动、不缩放、不旋转、不删除、不组合/解组。
4. **可改**：文字内容；字体、字号、字重、颜色、对齐、行距、缩进、项目符号；异常值用 `#EE0000`。
5. **可改**：既有表格（内容/行列/格式/位置/大小）与既有图片（替换/裁剪/缩放/移动）。
6. **可增**：图片、表格、文本框、形状，放在不遮挡既有内容处；新增对象字体族与字号与全篇一致，正文不低于 16 pt。
7. **溢出**：调该文本框字号（≥16 pt）/行距，或新增一页（复制既有页保留版式）；不要移动原对象"腾地方"。
8. **内容**：可生成/补全教学病例；如用真实资料必须先脱敏。
9. **措辞**：幻灯片上不出现"教学病例模拟数据/仅用于演示"等字样。
10. **命名**：`NAME-科室-病例汇报.pptx`；交付后删除 `_working` 副本。
11. **核对**：改后确认骨架（主题、母版、版式、页序、动画）与"非表格/图片原对象"的几何均未变。

## 不要做

- 不要换主题、母版、版式、背景、页序，不要增删动画。
- 不要移动/缩放/删除非表格/图片的原有对象。
- 不要用"整体重写形状文本"的接口后不检查格式是否被重置。
